<form action="/post/edit" method="POST">
    <label for="post-title">Post Title</label>
    <input type="text" name="post-title" id="post-title" value="{{{post.name}}}">
    <label for="post-content">Post Content</label>
    <input type="text" name="post-content" id="post-content" value="{{{post.content}}}">
    <input type="hidden" name="post-id" id="post-id" value="{{{post._id}}}"> 
    <button type="submit">Submit</button>
</form>

{{!-- <form action="/class/edit/tags" method="POST">
    <label for="tag-input">Tags</label>
    <input name="tag-name" id="tag-input" type="text">
    <input type="hidden" name="class-id" id="class-id" value="{{{class._id}}}">
    <button type="submit">Add tag</button>
</form>
<ul>
    {{#each class.tags}}
    <li>{{this}}</li>
    {{/each}}
</ul> --}}